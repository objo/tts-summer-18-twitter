<% @users.each do |user| %>
  <div class="user">
    <%= render "shared/avatar", user: user, width: "100px" %>
    <p><%= link_to "@#{user.username}", user_path(id: user.id) %></p>
  </div>
<% end %>
